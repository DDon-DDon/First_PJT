---
name: dev-pipeline-orchestrator
description: 개발 파이프라인 전체를 지휘하는 메타 스킬. 10개의 하위 스킬을 조율하며 로드맵 → 태스크 → 구현 → 리뷰 → 커밋 사이클을 관리함. (1) "파이프라인 시작", "개발 시작해줘" 요청 시, (2) "현재 상태 보여줘", "어디까지 했지?" 확인 시, (3) "다음 뭐 해?", "이어서 해줘" 요청 시 트리거. 상태 파일(.pipeline/state.json)로 세션 간 연속성 유지.
---

# Dev Pipeline Orchestrator

개발 파이프라인을 총괄하는 메타 스킬. 10개의 하위 스킬을 조율하며 체계적인 개발 워크플로우를 진행.

## 파이프라인 개요

```
┌─────────────────────────────────────────────────────────────────┐
│                     Development Pipeline                        |
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  로드맵 ──► 태스크 Loop ──────────────────────────► Phase 완료      │
│             │                                         │         │
│             ▼                                         │         │
│  ┌─────────────────────────────────────────────┐      │         │
│  │  계획 → 구현 → 리뷰 → 커밋 → 문서 → 검증          │ ────┘          │
│  └─────────────────────────────────────────────┘                │ 
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 하위 스킬 맵

| 단계 | 스킬 | 트리거 |
|------|------|--------|
| 계획 | `roadmap-task-splitter` | 로드맵/태스크 분해 |
| 계획 | `reference-reader` | 레퍼런스 문서 참조 |
| 계획 | `task-implementation-planner` | 구현 상세 계획 |
| 구현 | `test-writer` | 테스트 코드 작성 |
| 리뷰 | `code-reviewer` | 코드 리뷰 |
| 커밋 | `commit-message-writer` | 커밋 메시지 |
| 문서 | `doc-updater` | 체크리스트 업데이트 |
| 검증 | `task-validator` | 완료 조건 검증 |
| 전환 | `next-task-selector` | 다음 태스크 추천 |
| 전환 | `phase-summary` | Phase 완료 리포트 |

## 상태 관리

### 상태 파일 위치
```
project/
├── .pipeline/
│   ├── state.json          # 파이프라인 상태
│   └── history.json        # 완료 히스토리
└── docs/
    └── roadmap.md          # 로드맵 문서
```

### state.json 읽기
파이프라인 명령 수신 시 항상 먼저 상태 확인:
```bash
cat .pipeline/state.json
```

### state.json 업데이트
단계 전환 시 상태 파일 갱신:
```python
# 현재 단계 업데이트
state["pipeline"]["current_step"] = "reviewing"
state["context"]["last_skill"] = "code-reviewer"
```

## 워크플로우 상태 머신

### 태스크 상태
```
pending → planning → implementing → reviewing → committing → completed
```

### 상태별 액션

#### pending (대기)
```
액션: start_task
스킬: reference-reader → task-implementation-planner
출력: 구현 계획서
다음 상태: implementing
```

#### implementing (구현 중)
```
액션: 코드 작성 (사용자)
스킬: test-writer (필요시)
출력: 구현 코드 + 테스트
다음 상태: reviewing
```

#### reviewing (리뷰 중)
```
액션: review_code
스킬: code-reviewer → task-validator
출력: 리뷰 피드백
- 통과: → committing
- 수정필요: → implementing
```

#### committing (커밋 준비)
```
액션: prepare_commit
스킬: commit-message-writer
출력: 커밋 메시지
다음 상태: completed
```

#### completed (완료)
```
액션: finalize_task
스킬: doc-updater → next-task-selector
출력: 다음 태스크 추천
다음 상태: pending (다음 태스크)
```

## 명령어

### 파이프라인 시작
```
사용자: "백엔드 고도화 파이프라인 시작해줘"

액션:
1. .pipeline/ 디렉토리 생성
2. state.json 초기화
3. 로드맵 파일 경로 확인
4. roadmap-task-splitter 스킬 트리거
```

### 현재 상태 확인
```
사용자: "현재 상태 보여줘" / "어디까지 했지?"

액션:
1. state.json 읽기
2. 현재 Phase, Task, Step 파악
3. 진행률 계산
4. 다음 액션 제안
```

### 이어서 진행
```
사용자: "이어서 해줘" / "계속"

액션:
1. state.json에서 current_step 확인
2. 해당 단계의 스킬 트리거
3. 완료 후 다음 단계로 전환
```

### 태스크 완료
```
사용자: "A-1 완료" / "구현 끝났어"

액션:
1. task-validator로 완료 조건 검증
2. 통과 시:
   - commit-message-writer로 커밋 메시지
   - doc-updater로 로드맵 업데이트
   - state.json 업데이트
   - next-task-selector로 다음 추천
```

### Phase 완료
```
사용자: "Phase A 마무리해줘"

액션:
1. 모든 태스크 완료 확인
2. phase-summary로 리포트 생성
3. 다음 Phase 준비 안내
```

## 상태 파일 스키마

### state.json
```json
{
  "pipeline": {
    "id": "project-name-v1",
    "started_at": "2026-01-24T10:00:00Z",
    "roadmap_path": "docs/roadmap.md",
    "current_phase": "A",
    "current_task": "A-1",
    "current_step": "implementing"
  },
  "phases": {
    "A": {
      "name": "API 문서화 & DX",
      "status": "in_progress",
      "started_at": "2026-01-24T10:00:00Z",
      "tasks": {
        "A-1": {
          "status": "implementing",
          "started_at": "2026-01-24T10:30:00Z"
        },
        "A-2": { "status": "pending" }
      }
    }
  },
  "context": {
    "last_action": "구현 계획 생성",
    "last_skill": "task-implementation-planner",
    "last_output_path": ".pipeline/A-1-plan.md",
    "blockers": []
  }
}
```

## 스킬 조합 패턴

### 패턴 1: 태스크 시작
```
reference-reader
    ↓ (컨텍스트 추출)
task-implementation-planner
    ↓ (구현 계획)
→ 사용자에게 전달
```

### 패턴 2: 구현 완료 후
```
code-reviewer
    ↓ (리뷰 피드백)
task-validator
    ↓ (완료 조건 검증)
commit-message-writer
    ↓ (커밋 메시지)
doc-updater
    ↓ (체크리스트 갱신)
next-task-selector
    ↓ (다음 태스크)
→ 사용자에게 전달
```

### 패턴 3: Phase 완료
```
task-validator (전체 검증)
    ↓
phase-summary
    ↓ (리포트)
next-task-selector
    ↓ (다음 Phase 첫 태스크)
→ 사용자에게 전달
```

## 진행률 계산

```python
def calculate_progress(state):
    phase = state["phases"][state["pipeline"]["current_phase"]]
    tasks = phase["tasks"]
    
    completed = sum(1 for t in tasks.values() if t["status"] == "completed")
    total = len(tasks)
    
    return {
        "phase_progress": (completed / total) * 100,
        "completed_tasks": completed,
        "total_tasks": total
    }
```

## 출력 형식

### 상태 보고
```markdown
# 📍 파이프라인 상태

**프로젝트**: DoneDone 백엔드 고도화
**현재 위치**: Phase A > A-1 > implementing

## 진행률
```
Phase A ████████░░░░░░░░░░░░ 40% (2/5)
```

## 현재 태스크
- **A-1**: OpenAPI 스펙 강화
- **상태**: 구현 중
- **다음 단계**: 코드 리뷰

## 제안
"구현이 완료되면 '리뷰해줘'라고 말씀해주세요."
```

## 참고 파일

- `references/workflow.md`: 상세 워크플로우
- `references/state-schema.md`: state.json 전체 스키마
- `references/skill-triggers.md`: 스킬별 트리거 조건